---
title: "Efficient Fine-Tuning with BERT Adapters: A Network Science Perspective"
author:
  - name: W. H. W. Thompson
    affiliations:
      - name: University of Vermont
        department: Complex Systems and Data Science (CSDS)
    email: will.thompson@uvm.edu
    attributes:
      corresponding: true
abstract: |
  As language models continue to scale, full parameter fine-tuning becomes increasingly computationally expensive. Bottleneck adapters offer a parameter-efficient alternative by inserting small, trainable modules within the layers of a pre-trained model. In this paper, we investigate the effectiveness of BERT adapters for topic classification in network science research. We characterize the performance-compute trade-off and demonstrate that adapters can achieve competitive results while training less than 1% of the original model parameters.
keywords: [BERT, Adapters, Fine-Tuning, Computational Social Science, Network Science]
format:
  pdf:
    keep-tex: true
  html: default
bibliography: references.bib
---

# Introduction

Large-scale language models (LLMs) have revolutionized natural language processing (NLP). However, as mentioned in @bert2019, full parameter fine-tuning requires significant computational resources, especially when dealing with domain-specific datasets in fields like Network Science.

# Methodology

We utilize the bottleneck architecture described in @houlsby2019. This approach inserts two modules per transformer layer: one after the multi-head attention and one after the feed-forward layer. Each module consists of a down-projection, a non-linear activation, and an up-projection, effectively reducing the number of trainable parameters while maintaining representation quality.

# Results and Discussion

Our experiments on the CSDS research dataset indicate that BERT adapters converge faster than full fine-tuning. We find that the structural properties of the attention weights in adapted models exhibit higher modularity compared to their fully fine-tuned counterparts, suggesting that adapters preserve the original model's "backbone" more effectively.

![**Figure 1: ROC Curves Across Hyperparameter Sweep.** The curves demonstrate the robustness of BERT adapters across different bottleneck dimensions and learning rates. All configurations achieve an AUC \> 0.92, with the $d=512$ configuration showing the fastest convergence.](plots/fig1_roc_sweep.png){#fig-roc fig-align="center" width="80%"}

# Conclusion

We have demonstrated that BERT adapters are a highly efficient tool for scientific development in NLP. By minimizing the trainable parameter count, we enable more rapid iteration and experimentation on academic-scale hardware.

# References

::: {#refs}
:::